<div class="min-h-screen bg-gradient-to-br from-gray-900 to-pink-500">
  <div class="flex h-screen bg-gray-900 text-white">
    <!-- Sidebar Navigation -->
    <div class="w-16 md:w-64 bg-black flex-shrink-0 overflow-y-auto">
      <div class="p-4">
        <div class="flex items-center mb-6">
          <svg class="w-8 h-8 text-white" viewBox="0 0 24 24" fill="currentColor">
            <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 14.5c-2.49 0-4.5-2.01-4.5-4.5S9.51 7.5 12 7.5s4.5 2.01 4.5 4.5-2.01 4.5-4.5 4.5zm0-5.5c-.55 0-1 .45-1 1s.45 1 1 1 1-.45 1-1-.45-1-1-1z"/>
          </svg>
          <span class="text-xl font-bold ml-2 hidden md:block">Notava</span>
        </div>
      </div>
    </div>

    <!-- Main Content -->
    <div class="flex-1 flex flex-col overflow-y-auto px-4">
      <!-- Top Navigation Bar -->
      <div class="bg-gray-900 py-2 flex items-center justify-between border-b border-gray-800">
        <div class="relative flex items-center flex-1 max-w-3xl mx-4">
          <svg class="absolute left-3 w-4 h-4 text-gray-400" viewBox="0 0 24 24" fill="currentColor">
            <path d="M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"/>
          </svg>
          <input type="text" placeholder="Search for songs, artists, or albums" class="bg-gray-800 py-2 pl-10 pr-4 rounded-full w-full text-sm focus:outline-none focus:ring-1 focus:ring-blue-500">
        </div>
        <div class="flex items-center space-x-4">
          <% if user_signed_in? %>
            <%= link_to "Upgrade", plans_path, class: "bg-pink-500 hover:bg-pink-600 text-white font-semibold py-2 px-6 rounded-full transition" %>
            
            <!-- Profile Dropdown -->
            <div class="relative" data-controller="dropdown">
              <button data-action="click->dropdown#toggle" class="flex items-center space-x-2 text-gray-300 hover:text-white transition">
                <div class="w-8 h-8 rounded-full bg-gray-700 flex items-center justify-center">
                  <svg class="w-5 h-5" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 3c1.66 0 3 1.34 3 3s-1.34 3-3 3-3-1.34-3-3 1.34-3 3-3zm0 14.2c-2.5 0-4.71-1.28-6-3.22.03-1.99 4-3.08 6-3.08 1.99 0 5.97 1.09 6 3.08-1.29 1.94-3.5 3.22-6 3.22z"/>
                  </svg>
                </div>
              </button>

              <!-- Dropdown Menu -->
              <div data-dropdown-target="menu" class="hidden absolute right-0 mt-2 w-48 bg-gray-800 rounded-lg shadow-lg py-1 z-50">
                <div class="px-4 py-2 text-sm text-gray-300 border-b border-gray-700">
                  <%= current_user.email %>
                </div>
                <%= link_to "Profile", profile_path, class: "block px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white transition" %>
                <%= link_to "Settings", "#", class: "block px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white transition" %>
                <div class="border-t border-gray-700 my-1"></div>
                <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "w-full text-left px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white transition" %>
              </div>
            </div>
          <% else %>
            <%= link_to "Sign In", new_user_session_path, class: "text-gray-300 hover:text-white transition" %>
            <%= link_to "Upgrade", plans_path, class: "bg-pink-500 hover:bg-pink-600 text-white font-semibold py-2 px-6 rounded-full transition" %>
          <% end %>
        </div>
      </div>

      <!-- Profile Content -->
      <div class="py-8">
        <!-- User Info Section -->
        <div class="bg-gray-800 rounded-lg p-6 mb-8">
          <div class="flex items-center space-x-6">
            <div class="w-24 h-24 rounded-full bg-gray-700 flex items-center justify-center">
              <svg class="w-12 h-12 text-gray-400" viewBox="0 0 24 24" fill="currentColor">
                <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 3c1.66 0 3 1.34 3 3s-1.34 3-3 3-3-1.34-3-3 1.34-3 3-3zm0 14.2c-2.5 0-4.71-1.28-6-3.22.03-1.99 4-3.08 6-3.08 1.99 0 5.97 1.09 6 3.08-1.29 1.94-3.5 3.22-6 3.22z"/>
              </svg>
            </div>
            <div>
              <h1 class="text-2xl font-bold"><%= @user.username || @user.email.split('@').first %></h1>
              <p class="text-gray-400"><%= @user.email %></p>
            </div>
          </div>
        </div>

        <!-- Favorite Songs Section -->
        <div class="mb-8">
          <div class="flex items-center justify-between mb-4">
            <h2 class="text-xl font-bold">Favorite Songs</h2>
          </div>
          <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-4">
            <% if @favorite_songs.any? %>
              <% @favorite_songs.each do |song| %>
                <%= render 'shared/song_card', song: song %>
              <% end %>
            <% else %>
              <p class="text-gray-400 col-span-full text-center py-4">No favorite songs yet</p>
            <% end %>
          </div>
        </div>

        <!-- Recently Played Section -->
        <div class="mb-8">
          <div class="flex items-center justify-between mb-4">
            <h2 class="text-xl font-bold">Recently Played</h2>
          </div>
          <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-4">
            <% if @recent_tracks.any? %>
              <% @recent_tracks.each do |track| %>
                <%= render 'shared/song_card', song: track %>
              <% end %>
            <% else %>
              <p class="text-gray-400 col-span-full text-center py-4">No recently played tracks</p>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div> 